
Dependencies
============

1. ocaml(>=4.02.3)
2. findlib
3. pcre(pcre-ocaml)
4. uutf(>=0.9.4)
5. uucp
6. ounit (>=2.0.0, for test)

Important notice
================

pcre library(http://www.pcre.org/) must be build with configuration "--enable-utf8".

Installing
==========

(1) Install in manual way.

make
sudo make install

(2) Install by opam.

opam install jingoo
